# RISHITA // DATA UNIVERSE

A 3D spatial portfolio experience built with **React, Three.js (React Three Fiber), GSAP, and Tailwind CSS**.

## Installation & Local Execution

1. Initialize project and install dependencies:
```bash
npm install